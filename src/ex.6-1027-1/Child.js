function Child({ cat, name, price }) {
  // const {cat, name, price} = props
  return (
    <>
      <div>category: {cat}</div>
      <div>name: {name}</div>
      <div>price: {price}</div>
    </>
  )
}

export default Child
